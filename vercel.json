{
  "buildCommand": "cd dashboard && npm install && npm run build",
  "outputDirectory": "dashboard/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://tcheck-backend-production.up.railway.app/api/$1" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
